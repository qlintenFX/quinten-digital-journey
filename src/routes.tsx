import React from 'react';

const routes = {
  about: "/about",
  projects: "/projects",
  contact: "/contact",
  blog: "/blog"
};

export const RouteComponents: Record<string, React.FC> = {};

export default routes; 